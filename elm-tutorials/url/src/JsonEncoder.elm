module JsonEncoder exposing (..)

{-| Korban: Encode Json
    -------------------
    @ https://shorturl.at/WSMJV

    What about the opposite conversion, from Elm values to a JSON string?
    In JavaScript, converting JSON to a string is as simple as
    `JSON.stringify(myObject)`, but in Elm it takes a bit more work.
-}

